<Window x:Class="ImageLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageLab"
        xmlns:uc="clr-namespace:ImageLab.UserControls"
        xmlns:vm="clr-namespace:ImageLab.ViewModels"
        xmlns:models="clr-namespace:ImageLab.Models"
        xmlns:conv="clr-namespace:ImageLab.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <GridLength x:Key="TreeToggleWidth">20</GridLength>
        <conv:LevelConverter x:Key="LevelSpaceConverter" LevelWidth="{StaticResource TreeToggleWidth}"/>
        <conv:VisibilityConverter x:Key="TreeToggleConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Name="Options" Grid.Column="0" Content="Options"
                    Command="{Binding SelectOptionsCommand}"/>
        </Grid>
        
        <DataGrid AutoGenerateColumns="False" Grid.Row="1" CanUserAddRows="False"
                  ItemsSource="{Binding Model.FlatModel}">
            <DataGrid.Columns>
                <DataGridTemplateColumn
					Header="Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding Level, Converter={StaticResource LevelSpaceConverter}}"/>
                                    <ColumnDefinition Width="{StaticResource TreeToggleWidth}"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox 
									Grid.Column="1"
									IsChecked="{Binding IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
									Visibility="{Binding HasChildren, Converter={StaticResource TreeToggleConverter}}"/>
                                <TextBlock Grid.Column="2" Text="{Binding Name}"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" IsReadOnly="True"/>
                <DataGridTextColumn Header="BMP" Binding="{Binding BMPDetails}" IsReadOnly="True"/>
                <DataGridTextColumn Header="PNG" Binding="{Binding PNGDetails}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
</Window>
