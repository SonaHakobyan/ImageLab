<UserControl x:Class="ImageLab.UserControls.Tools"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageLab.UserControls"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary Source="..\Resources\Styles.xaml"/>
    </UserControl.Resources>
    
    <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Name="Options" Content="Options" Style="{StaticResource ButtonStyle}"/>
        <ComboBox Grid.Column="2" Name="Formats" Text="Selcet Format" Style="{StaticResource ComboBoxStyle}"/>
        <Button Grid.Column="3" Name="Convert" Content="Convert" Style="{StaticResource ButtonStyle}"/>
    </Grid>
</UserControl>
